# List of packages to be installed installed on the target hosts and the
# related services to be started.
packages:
  - name: httpd
    service: httpd

  - name: mariadb-server
    service: mariadb

  - name: firewalld
    service: firewalld
